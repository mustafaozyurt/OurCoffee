const StyleItem = ({ width, height, marginTop, marginBottom }) => {
  return (
    <div
      style={{
        width: width,
        height: height,
        background: "#e68541",
        marginTop: marginTop,
        marginBottom: marginBottom,
      }}
    ></div>
  );
};

export default StyleItem;
